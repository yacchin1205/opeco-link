# opeco.link Concept

## 概要

opeco.link は、Agent のセッションと手元のデバイスグループを一時的な暗号関係で結び、作業の状態を確認したり、必要な判断やメッセージを返したりするためのサービスである。サービス名とドメインは `opeco.link`、CLI の実行名は `opeco` とする。

通知の送り手は主に Agent であり、CLI からの利用も想定する。Agent は MCP を通じて独立したセッションを作成する。

本サービスはユーザーアカウントを持たない。セッションは特定の人に所有されず、デバイスグループとの関係は鍵の交換によってのみ成立する。

## セッション

セッションは、一連の対話や作業を表す独立した単位である。たとえば、ひとつの Agent セッションが、デバイス上ではひとつのカードとして見える。

複数のセッションが並列に存在できる。セッションから新しいイベントが届くたびにカードを増やすのではなく、同じセッションのカードを更新し、その最新状態や最新通知を表示する。

セッションの管理主体は、そのセッションを作成したプロセスである。作成元プロセスはセッション全体を閉じることができるが、管理主体の引き継ぎや回復は行わない。

セッションは短命であり、最後の活動から1日程度で失効する。作成元のプロセスが消えた場合も、最後の活動を基準として自然に失効する。閉じられた、または失効したセッションは端末上からも消え、必要であれば新しいセッションを作成する。

最後の活動とは、作成元プロセスがセッションを作成した、またはセッションを更新した時点を指す。デバイスからの応答、カードの閲覧、通知の受信、および同期はセッションの期限を延長しない。応答を受け取った作成元プロセスがセッションを更新した場合は、その更新が新しい活動となる。
共有送信後に、サーバ側で延長された期限が端末側へ反映されないことは制約として許容する。期限延長の反映は望ましいが、その保証に伴う共有状態の書き込み・同期によるシステム複雑化は容認しない。

短命性は単なる保存期間ではなく、ユーザーアカウントや恒久的な所有権管理を持たないための基本特性である。

## デバイスグループとの関係

セッションとデバイスグループは、QR コードなどを介したワンショットの鍵交換によって関係を結ぶ。関係の成立に、セッションの作成元による個別の明示的な承認は必要としない。鍵交換が成立した時点で、そのデバイスグループはセッションに参加する。

未参加のセッションを一覧、検索、または発見する手段は設けない。デバイスグループがセッションへ参加する方法は、QR コードなどを介した鍵交換に限られる。このため、セッションへの参加申請や承認待ちという状態、および承認する対象は存在しない。

セッションは、参加したデバイスグループが意図した相手かどうかを識別できない。このため、特定のデバイスグループだけを参加解除することはできない。関係を無効にする必要がある場合は、作成元プロセスがセッション全体を閉じる。

通知を受け取ることや応答を返すことは、ユーザーIDではなく、対応する鍵を所持していることによって可能になる。個々のデバイスは、所属するデバイスグループを通じてセッションと関係する。

複数のデバイスをひとつのデバイスグループとして扱える。新しいデバイスをグループへ追加するには、既存デバイスによる承認を必要とする。デバイスの削除も既存デバイスから行える。

ひとつのデバイスが同時に属するデバイスグループは高々ひとつとする。デバイスグループを、その端末が通知を受け取り応答する際の一貫した暗号上の主体にするためである。単独の端末も暗号上はひとつのデバイスグループとして扱うが、利用者にグループの作成を要求せず、複数端末になったときだけ「共有」として表現する。複数人で同じセッションを見る場合は、同じグループへ人を追加するのではなく、それぞれのデバイスグループをセッションへ参加させる。

デバイスグループには所有者や管理者を置かず、参加中の端末は等しい権限を持つ。ユーザーという恒久的な主体を導入せず、それでも意図しない端末追加を防ぐため、追加時だけは、参加を希望する端末が示す依頼を既存端末が読み取り、明示的に承認する。参加中の端末は他の端末との共有を解除でき、複数端末で共有している場合は自身も共有から離脱できる。離脱または削除された端末は単独利用へ戻る。単独利用の端末には離脱という操作を設けない。

自己離脱は拒否や承認の対象ではなく、その端末を有効なメンバーから外す。離脱端末を含む鍵は、離脱後に作成されるイベントには使用しない。

デバイスグループには明示的な寿命を定めない。すべてのデバイスでグループを操作するための鍵が失われても、グループが失われたという明示的な状態にはならず、単に操作できなくなる。操作不能になったグループは復旧せず、必要であれば新しいグループを作成する。

ひとつのセッションは、複数のデバイスグループと個別に関係を結べる。これにより、複数人が同じセッションのカードを見ることができる。ただし、サービス上に人やユーザーを表す主体は導入しない。

また、ひとつのデバイスグループから複数のセッションを見ることができる。セッションとデバイスグループの関係は多対多であり、セッションは誰が見ているかを知らない。

## 暗号化とサーバの役割

通知本文、応答本文、添付および添付manifestなどのペイロードは暗号化されたまま扱われ、サーバはその内容を読まない。大きな添付も平文には戻さず、暗号文だけをobject storageへ置く。

**関連するすべての秘密鍵の平文はデバイスにしか存在せず、サーバは秘密鍵の平文を知ることができない。**

端末の追加や削除後もこの性質を保つため、デバイスグループは初回作成を起点とする署名済みの鍵・メンバー遷移チェーンを持つ。各遷移は直前の遷移hash、全メンバーの端末公開鍵、全鍵パッケージのdigestを結び付け、操作端末の署名と直前グループ鍵による連続性署名を必要とする。遷移hashは署名表現ではなく正規化した遷移本文から計算し、ECDSA署名の別表現がforkを作らないようにする。セッション参加時のワンショット鍵交換は、その時点の遷移hashも認証する。端末は一度信頼した遷移が欠落、改変または巻き戻された場合には処理を継続しない。

グループ内でセッションを別端末へ継承するときは、セッション作成元の公開鍵を含む記述子を、参加した端末のidentity鍵とその時点のグループ継続鍵で署名する。各端末は両署名に加え、作成元公開鍵が実際にP-256曲線上のECDH鍵であり、署名端末が参加時点の認証済み遷移に同じidentity公開鍵を持つメンバーとして含まれていたことを同期のたびに検証し、保存済みの作成元公開鍵とも照合する。この記述子はデバイスではなくグループがセッションへ参加した事実を表すため、署名端末が後に離脱または削除されても無効にならない。検証または照合できないセッションを表示せず、保存済みのものも失効させて、relayによる作成元公開鍵の差替えを暗号化先として採用しない。端末からの新しい応答は常に現在の利用可能な鍵epochを使い、削除前の旧鍵で削除後の応答を作らない。

別の端末を削除するときは、残存端末が新しい共有鍵を含む遷移を作る。端末自身が離脱するときは、離脱端末が旧鍵のまま自身を外す遷移だけを署名し、その間はイベント送信を停止する。残存端末がその遷移を検証した後に、同じ残存メンバーへ新しい共有鍵を作って送信を再開する。これにより、離脱端末自身に離脱後の鍵を決めさせない。削除済み端末が過去に受け取った内容を取り戻すことはできない。

ただし、relayが正しい新headを最初から隠して端末を最後に見たheadへ固定するfreezeや、共通headから同時に作られた正当な遷移を別々の端末へ見せ続けるforkは、relay以外のgossip、quorum、またはfreshnessの根拠を持たないこの設計だけでは検出できない。ここで保証するのは、一度観測したheadの巻き戻し・改変の拒否と、観測した削除後に旧epochを新規送信へ使わないことである。

この旧epoch使用禁止の例外として、共有拡張が本体の更新途中の鍵・宛先を読む場合や、読取後に本体が端末削除を観測しても拡張が旧鍵で新しい内容を暗号化する競合は、稀な状況と判断して許容し、その防止だけを目的とする検証完了フラグ・更新途中の判別・プロセス間の実行調整は追加しない。正常なrelayは旧epochの送信を拒否するが、悪意あるrelayが暗号文を旧鍵を持つ削除済み端末へ渡す場合の秘密保持は、この競合では保証しない。

通知およびセッションの情報は恒久的に保存せず、セッションが失効すると端末上のカードも含めて消える。アカウント回復や、失われた鍵および関係の復旧は行わない。

## 通知と表示

OS の通知欄には、`notify` では新しい通知があること、`request` では入力を求めていることのみを表示する。`status` では既定で OS 通知を出さない。通知本文や質問は表示せず、暗号化されたまま扱う。通知元の識別名を表示するかどうかは未決定である。

端末は、参加中のセッションを注目対象にできる。注目中の端末には、`status` でも状態が更新されたことだけを OS 通知で知らせる。注目は端末ごとの選択であり、dismiss のようにデバイスグループ内で共有しない。同じグループの端末でも、手元の端末で逐一知りたいかどうかは端末ごとに異なるためである。注目はセッションとともに失効し、解除は端末が自分で行う。

対応を要する `notify` と `request` の件数はデバイス間で共有し、OS のバッジにも反映する。いずれかのデバイスで選択またはdismissされた項目は、他のデバイスでも対応不要として扱う。

アプリでは、セッションごとにカードを表示する。カードには、そのセッションの最新状態と最新通知を表示し、新しくイベントを受け取ったカードを上に並べる。

## イベント

セッションでは、次のイベントを扱う。

- `notify`: 通知を送る。

- `status`: セッションの現在状態を更新する。

- `request`: 判断や操作を求める。

- `response`: `request` に対する操作結果を返す。

- `feedback`: デバイスから、本文と複数の添付を持つ汎用メッセージを返す。本文と添付の少なくとも一方を必要とする。

最初の添付種別は写真とする。端末側で向きを確定し、表示や転送に過剰な解像度を落としてから暗号化する。添付の有無と暗号文の大きさは中継サーバから観測できるが、画像の種別、寸法および内容は暗号化manifestの内側に置く。

## request / response

`request` は Go / NoGo など、複数の選択肢を持つことができる。`response` はデバイスからセッションの作成元プロセスへ返される。

本サービスは、複数のデバイスまたはデバイスグループから返された `response` をひとつに束ねず、受け取ったものをすべて作成元プロセスへ渡す。先着順による選択や回答の集約は行わない。一方、いずれかのデバイスから選択またはdismissを受け取った項目は、他のデバイスでも対応不要として表示する。

各 `response` の内容をどのように解釈するかは、Agent などの作成元プロセスが決める。

作成元プロセスが `request` を完了させると、その終了はすべての参加デバイスへ伝わる。
