# 実装が Single Source of Truth。この文書には Why だけを書く。

## 中核基盤に Cloudflare を選ぶ

opeco.link の中核基盤には Cloudflare を採用する。

Firebase には利用経験と実績があり、特にモバイルとの統合に強みがある。一方、opeco.link の中心にあるのは、ユーザーアカウントを持たない、短命で独立したセッションの双方向な中継である。この性質には Cloudflare の提供する仕組みがよく対応する。

また、本プロジェクトには Cloudflare を技術的に調査し、その適性を確かめる目的もある。既知の技術による開発速度だけでなく、新しい基盤についての知見を得る価値を選択理由に含める。

中核基盤と OS 通知の配送路は分けて考える。Cloudflare を中核に選ぶことは、FCM、APNs、WNS を排除することを意味しない。OS 通知は端末へ更新の存在を知らせるための配送路であり、セッションやイベントの本体を担う基盤ではない。

## 検証用クライアントに PWA を選ぶ

最初のクライアントには PWA を採用し、鍵はブラウザ内で生成して IndexedDB に保存する。

この PWA は本番クライアントの最終形ではなく、コンセプトの確認と開発のためのものである。ネイティブアプリを用意する前に、アカウントを持たない鍵交換、セッションのカード表示、通知、および双方向のイベントが一連の体験として成立するかを確認したい。

ブラウザ内の鍵は、サイトデータの削除などによって失われる可能性がある。この段階では回復手段を設けず、鍵を失った場合は新しいデバイスグループを作る。これは、回復を行わない現在のコンセプトとも一致する。

Passkey やクラウド上の鍵バックアップは、最初の検証には導入しない。回復や長期利用の仕組みを先に加えると、短命なセッションと鍵の所持だけでサービスが成立するかという、確認したい中心部分が不明瞭になるためである。

Web クライアントは、配信される JavaScript を信頼する必要があり、ネイティブアプリと同じ強さの信頼境界にはならない。この制約は検証用という位置付けで許容し、PWA で得た結果をそのまま本番環境のセキュリティ保証とはみなさない。

## CLI は Go の単一バイナリとする

CLI は Go で作り、単一バイナリとして配布できる形を選ぶ。

CLI はセッションを作成し、その存続中にイベントの送受信を担う主体である。追加のランタイムを利用者に要求せず、主要なデスクトップ環境で同じものを長時間動かせることを重視する。また、暗号処理、ネットワーク接続、イベント送受信の並行処理を一つのプロセスで扱う用途にも Go は適している。

イベント送信は At Least Once とし、一時的な通信障害には同じイベントをそのまま再送する。サーバは記録済みのイベントの再送を成功として扱うため、送信側は記録されたかどうかを知るための状態管理を持たずに済み、再送によって同じイベントが二重に届くこともない。

CLI が動作する端末のネットワークはスリープなどで一時的に途切れ得るため、短い応答時間よりもその断絶を越えて通知を届けることを優先する。

同じ CLI から、人が直接操作する開発用の利用方法と、Agent が MCP を通じて操作する利用方法の両方を提供する。二つを別の実装にしないことで、手動で確認した振る舞いと Agent が利用する振る舞いが同じセッションモデルに基づく状態を保ちたい。

MCP は CLI を Agent から利用するための入口であり、MCP の接続そのものを opeco.link のセッションとはみなさない。セッションは Agent と追跡中の作業を識別するため、一つの MCP プロセスでは一つのセッションを再利用する。接続やプロセスの終了で未読の内容を即座に消さないよう、セッションには独立した識別と寿命を持たせる。

既定の CLI は `eval "$(opeco --title ...)"` に渡す環境変数の設定だけを標準出力へ書き、終了する。参加用 URL と端末 QR は標準エラー出力へ分ける。以降の `opeco notify` などは `OPECO_SESSION_FILE` が示す一時状態を引き継ぐため、シェルを操作する間 CLI を常駐させる必要はない。従来の対話モードは `--interactive`、MCP は `mcp` として明示する。

対話モードと MCP の秘密鍵はプロセスのメモリだけに保持する。シェルモードだけは、秘密鍵、参加用秘密、認証済みの鍵履歴、未終了の質問と応答カーソルを OS の一時ディレクトリへ保存する。POSIX 環境ではディレクトリを `0700`、ファイルを `0600` とし、環境変数には秘密そのものではなくパスとセッション ID を渡す。別コマンドへの引き継ぎはアカウントや鍵の回復機構ではなく、同じローカル作業の継続である。

各コマンドは読取から通信、書戻しまで排他的にロックする。鍵履歴の巻き戻しや応答の重複取得を避けるため、送信に失敗しても観測済みの認証状態は保存し、ファイルは一時ファイルから置換する。明示的に閉じた場合と失効を検出した場合に状態と添付を削除する。シェルを終了しただけでは削除せず、残ったファイルは通常の OS 一時領域の掃除に委ねる。安全な消去や、失った状態の復旧は保証しない。

CLI プロセスの通常終了は、セッションを明示的に閉じる操作とは区別する。プロセスが予期せず失われても、閲覧側のカードを即座に消すのではなく、最後の活動から定められた寿命が尽きるまで残すためである。即時に無効化したい場合だけ、セッションを明示的に閉じる。

`request` への応答は CLI が一つに決定せず、受け取ったものを Agent に渡す。どの応答を採用し、その後の状態をどう更新するかは Agent の責務であり、CLI はサービスの意味を解釈する主体にはならない。

## QR 画像はローカルプロセスのメモリから表示する

端末に描画した QR コードは、表示経路による文字幅や行位置の変化によって読み取れないことがある。一方、鍵交換情報を画像ファイルとして保存すると、セッションより長く秘密が残る可能性が生じる。このため、対話モードと MCP はループバック接続だけで到達できる短命な画像表示を提供し、QR 画像をプロセスのメモリだけに保持する。即時終了するシェルモードは画像サーバを起動せず、端末 QR と参加 URL を使う。

画像のローカル URL は鍵交換情報から独立したランダム値とし、ブラウザにキャッシュさせない。URL が履歴に残っても、CLI の終了または表示期限の経過後には意味を持たず、URL 自体からセッションや鍵交換情報を復元できない状態にする。

CLI が別ホストやコンテナで動く場合、閲覧側のブラウザからそのループバック接続へ到達できない。ローカル画像表示を唯一の参加経路にはせず、端末 QR コードと鍵交換 URL を残すことで、実行環境による到達性の違いと鍵交換の方式を分離する。

## セッションごとに Durable Object を割り当てる

各セッションには、SQLite を保存領域とする独立した Durable Object を割り当てる。

セッションは互いに独立しており、順序付け、参加関係、応答、および寿命もセッション内で完結する。この境界を Durable Object の一貫性境界と一致させることで、複数セッション間の調整や中央のセッション一覧を必要としない構造を保つ。

オブジェクトの個数自体ではなく利用量が費用を決めるため、短命なセッションを個別のオブジェクトに分けることによる費用上の不利益は小さい。失効時には個々の行を消すだけでなく、その Durable Object の保存領域全体を破棄し、空の SQLite データベースが残り続けないようにする。

リアルタイム接続を導入する場合は、アイドル中の接続によって Durable Object の稼働時間が増え続けない方式を必須とする。

## 添付暗号文は private R2 に分離する

写真は短いイベント本文より大きくなり、Durable Object のSQLiteへ入れると応答一覧の読み書き、保存量、将来の上限変更を密結合にする。このため、Durable Objectには添付の識別子、所有関係、長さ、checksum、状態およびR2 object keyだけを置き、添付暗号文そのものはprivate R2 bucketへ分離する。object keyは外部の識別子から推測せず、R2に公開URLを持たせない。

アップロードは予約、暗号文upload、応答commitの三段階にする。先に大きなobjectを受け取ってから所有関係を判断せず、応答だけが保存されて対応するobjectがない状態も避けたい。現在のサイズ上限はプロトコルに固定せず、予約応答でサービス方針として返す。これにより、利用量や運用費用を見て上限を変更しても、新しいプロトコル世代を必要としない。

複数画像は表示順のmanifest配列とID配列を一つの応答へ含め、全画像を同時に公開することで、本文の「1枚目、2枚目」という参照を受信側まで保つ。

添付はイベント本文用の共有鍵をそのまま流用せず、同じECDH secretから添付専用のHKDF contextで鍵を導出する。session、group、鍵timestamp、response IDおよびattachment IDを鍵導出とAES-GCMのadditional dataへ結び付け、別の応答や添付への置換を復号時に失敗させる。manifestも応答本文の暗号文内に置き、relayがmedia typeや画像寸法を解釈する必要をなくす。

作成元は暗号文のchecksumとGCM tagを検証し、manifestとの一致を確認した後にだけ平文をOSの一時領域へ書く。MCPへはそのローカルファイルをresource linkとして渡す。平文をAgentプロセスのヒープに長く保持しない一方、通常の一時ファイルはsecure eraseを保証せず、異常終了時の残存はOSの一時領域管理に委ねる。このローカルendpointが侵害された場合はE2E暗号化の保証範囲外である。

## デバイスグループも独立した一貫性境界にする

デバイスグループにはセッションより長い寿命があり、複数の短命なセッションから同時に参照されるため、セッションとは別の Durable Object を割り当てる。端末追加の承認、削除、およびグループ鍵を同じ保存境界に置き、ユーザーや中央のグループ一覧を導入せずに整合性を保つ。

端末ごとの鍵とデバイスグループの共有鍵は役割を分ける。端末ごとの鍵は、どの参加端末が管理操作を行ったかを証明し、共有鍵は、同じグループへ送る暗号文を端末数に比例して複製せずに共有するためである。

グループ鍵とメンバー集合は、初回作成をrootとする署名済み遷移チェーンにする。遷移は直前hash、timestamp、操作端末、共有公開鍵、全端末の署名・暗号化公開鍵、および端末別鍵パッケージのdigestを正規化して結び付ける。遷移hashはこの正規化本文だけから計算し、署名bytesはhashへ含めず別途検証する。これによりECDSAの等価な署名表現を別headとして扱わない。操作端末のidentity署名に加えて直前共有鍵による連続性署名を検証し、セッション参加時にはワンショットHMACでその時点の遷移hashをanchorする。端末は保存済みheadを含まない履歴、署名不正、メンバー不一致をfail closedとする。

グループへ参加したセッションは、session ID、group ID、protocol version、作成元ECDH公開鍵、参加時の鍵timestamp・遷移hash、および署名端末IDを含む記述子として保存する。参加端末のidentity署名と参加時グループ鍵の連続性署名を必須とし、各端末は同期のたびに作成元公開鍵が実際のP-256曲線点であることと、署名者が参加時点の認証済み遷移に同じidentity公開鍵を持つメンバーとして含まれていたことを確認して両署名を検証する。認証済み記述子の作成元公開鍵が保存済みsession recordと一致するセッションだけを維持・継承する。記述子はグループのセッション参加を証明するため、署名者が後に離脱または削除されても失効させない。不一致や重複IDを持つセッションはローカルでも失効する。不正な1件の記述子や鍵導出失敗で、ほかの失効処理と永続化を止めてはならない。応答系の新規暗号文は現在の利用可能なheadだけで作り、relayと削除済み端末が共有するhistorical keyでの送受信を拒否する。

グループ同期・端末削除と、応答・dismiss・feedbackなどのセッション操作は、同一プロセス内で相互排他にする。新しいheadを保存してから古いsession recordを失効させるまでの途中状態を送信処理へ見せず、送信中に同期が古いrecordを再保存する競合も許さない。

共有拡張は本体が保存した鍵・宛先を読み、鍵同期や共有状態の書き戻しを担わない方針とする。更新途中の読取や読取後に本体が削除を観測する競合はCONCEPTの制約として許容し、検証完了フラグ・更新途中の判別・プロセス間排他を追加しない。稀な競合への対処で、本体と拡張の実行調整・状態管理を複雑化させないためである。

同じグループへのイベント暗号文は使用する共有鍵ごとにひとつとし、共有鍵そのものだけを各端末へ個別に渡す。複数端末という利便性によって、イベント保存量と作成元の暗号処理が端末数に比例することを避けたい。

自己離脱は二段階にする。離脱端末は旧共有鍵のまま自身を除いたメンバー集合を署名する「除外マーカー」だけを作り、サーバはこのマーカーがheadの間はイベントに使える鍵を返さない。残存端末が履歴を検証してから、同じメンバー集合に対する新しい共有鍵を署名し、送信を再開する。別端末の削除は残存する操作端末が新鍵を含む一つの遷移として行う。

relay外の観測経路を持たないため、relayが新headを一度も見せないfreezeと、共通headからの正当な同時遷移を端末ごとに分けるforkは検出対象外である。一度保存したheadの欠落・巻き戻し・改変は拒否するが、強い最新性や全端末の単一viewは主張しない。

## 暗号上のグループを利用者の操作単位にしない

単独端末も暗号上はひとつのデバイスグループとして扱うが、グループ作成は利用者の目的ではないため、明示的な初期設定にはしない。利用者に見せるのは、単独利用か、複数端末で通知を共有しているかという違いだけにする。

参加依頼と承認は一時的な手続きとして扱い、端末の削除や共有解除は通常の通知画面から分離する。暗号プロトコルの状態や危険を伴う管理操作を常時表示せず、その時点の判断に必要な情報だけを提示したい。

## iOS クライアントは端末内の信頼境界を使う

iOS クライアントはネイティブアプリとし、デバイスグループの秘密鍵と参加中セッションの鍵は、その端末だけで利用できる Keychain に保存する。

PWA はコンセプト検証に適している一方、配信される JavaScript を毎回信頼する必要がある。ネイティブアプリでは、署名されたアプリと OS の保護領域を信頼境界にすることで、サーバから配信されるコードが端末鍵へ触れる構造を避けたい。

Macの共有拡張も同じKeychain access groupを使うため、Mac本体の保存先はData Protection Keychainとする。

鍵のクラウド同期や回復は行わない。端末上の鍵を失うと操作不能になることを受け入れることで、ユーザーアカウントを導入せず、鍵の所持を関係の根拠とするコンセプトを保つ。

## OS 通知には更新の存在だけを載せる

iOS の OS 通知には APNs を利用するが、暗号化されたイベント本体の配送路にはしない。APNs が運ぶのは更新の存在と、通知、入力要求、状態更新の区別だけであり、アプリはセッション経路から暗号文を取得して端末内で復号する。状態更新だけでは、端末が注目を指定しない限り OS 通知を出さない。

APNs のデバイストークンはセッションごとに複製せず、デバイスにひとつだけ保持する。セッションより長く存続し得る配送先を短命なセッションへ繰り返し登録せず、更新時は同じデバイスの現在値を置き換える。

OS のバッジとデバイス間のdismiss共有には、内容を表さない項目IDだけを平文で扱う。サーバが暗号化本文を解釈することなく、同じ項目が対応を要するかどうかとデバイス単位の件数だけを共有するためである。選択結果とdismissの暗号文は、この状態変更とは別にすべて作成元へ渡す。

イベントの保存と APNs の外部配送は同じ成功条件にしない。イベントを受理した後の APNs 障害によって、作成元がイベント自体の成否を判断できなくなることを避けるためである。OS 通知はセッション内の短命な配送待ち状態として扱い、イベント本体とは独立して再試行する。

APNs の認証情報は通知ごとに作り直さず、安全な有効期間内で再利用する。外部サービスが要求する更新間隔を守り、通知数やセッション数の増加そのものが認証拒否を招かないようにするためである。

配送の失敗は通知ごとに分離する。恒久的な失敗が後続通知を止めず、一時的な失敗も無期限・高頻度には再試行しない。OS 通知は補助的な起床手段であり、その障害や再試行が暗号化イベント本体の保存と取得を妨げないことを優先する。

## 注目は端末単位でセッションに持つ

`status` を OS 通知にするかどうかは、受け取る端末が決める。作成元はどの端末が注目しているかを知らず、`status` の送り方も変えない。

サーバは本文を読めないので、`status` を配送するには作成元が平文の通知種別として `status` を添え、サーバが注目中の端末を知っている必要がある。注目中かどうかは、それ以外の端末状態と同じく内容を表さない平文のメタデータとして、セッションの Durable Object に端末単位で置く。前面にない iOS アプリを起こす手段はサーバからの push しかなく、端末内で完結させることはできない。

単位を端末にするのは、同じデバイスグループの Mac と iPhone で、逐一知りたいかどうかが異なるためである。dismiss がグループで共有されるのは対応不要という事実の共有であり、注目は端末の関心なので共有しない。注目はセッションとともに失効し、自動解除は設けない。

`status` は上書きされるので、その OS 通知も最新の一件だけに意味がある。配送待ちは同じ端末宛の古い `status` を新しいもので置き換え、APNs でもセッション単位の collapse identifier で畳む。頻度が高い前提で端末が選ぶものなので、`notify` や `request` と区別できるよう音は付けず、アプリが前面にある間は表示しない。

ブラウザ向けには push がなく、注目はブラウザには及ばない。

## セッション単位の同期失敗はそのカードに留める

端末はセッションごとに個別に同期するので、ひとつのセッションの同期が失敗しても他のセッションは正常に更新できる。この失敗を端末全体のエラーとして表示すると、正常なカードの操作まで妨げる。

このため、セッション単位の同期失敗はそのカードの中に表示し、端末全体の接続状態は変えない。復号の失敗のように次の同期で解消しない失敗も同じ扱いになり、そのカードには失敗の表示が残り続ける。端末を離脱させる失敗と処理の中断だけは、セッションの外で扱う。

## 既知の失効時刻を端末側でも信頼する

端末が最後に保存したセッション失効時刻を過ぎた場合は、サーバへの到達可否にかかわらず、そのセッションのカード、共有鍵、権限、復号済み内容をローカル保存から削除する。

作成元の活動によってサーバ上の寿命が延びていても、端末にその更新が保存されていなければ削除する。短命性を保証する場面では、到達できないサーバ上の状態を推測して秘密を長く保持するより、必要なら再参加することを選ぶ。

共有送信の応答で新しい期限を取得しても、その反映のために本体との共有状態を書き戻さず、期限更新は通常の同期に任せる。延長を取りこぼすことは許容し、その防止のための共有書き込み・同期機構は追加しない。
